<!doctype html>
<html lang="en-US">
  <title>A Sinatra CMS</title>
  <link rel="icon" href="favicon.ico">
  <link href="/stylesheets/cms.css" rel="stylesheet" type="text/css" />
  <body>
    <% if session[:message] %>
      <p class="message"><%= session.delete(:message) %></p>
    <% end %>
    <%= yield %>
  </body>
</html>